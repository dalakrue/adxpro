2026-06-12 Data Visualization News/NLP + KNN/Greedy Upgrade

- Adds one open/close section under Data Visualization:
  News NLP + KNN/Greedy Priority + Accuracy-Adjusted Projection.
- Uses Twelve Data/existing OHLC as the main market data source.
- Finnhub API key box added; FMP backup and RSS fallback included.
- News/NLP is confirmation/conflict filter only. It cannot override One Unified PowerBI regime.
- BEAR = SELL, BULL = BUY, RANGE = WAIT.
- Includes descriptive, predictive, prescriptive analysis using existing system data.
- Shows KNN/Greedy priority rows and Best Opportunity #1/#2.
- Adds accuracy-adjusted projection overlay/table using existing prediction + backtest error + news confirmation.
- No FinBERT, no Transformer NLP, no new heavy ML model.
- API/news calls run only after Run News/NLP is clicked and are cached.
- Copy Full and Data Visualization copy include the new News/NLP export.
- Finder mirrors the Data Visualization News/NLP priority after it has been run.
